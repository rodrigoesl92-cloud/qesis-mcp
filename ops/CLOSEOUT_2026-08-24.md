# Closeout 2026-08-24: the ecosystem runs itself, with three things still open

Written by the orchestrator at the operator's instruction to confirm the systemic
faults are closed, or to say plainly which are not. Both halves are below.

## What was systemically wrong, and what now prevents it

| Fault | Was | Now | Control |
|---|---|---|---|
| The ledger existed twice and the copies named different lessons under L-119 and L-120 | 2 files, 141 and 120 max, silent divergence for 4 days | one file, mirrored byte-identical, 140 entries | `verify_ledger_singleton.py`, 5 V-2 fixtures, in the self-heal control set |
| Lesson ids allocated from the tail of a file that is not sorted | L-142 nearly overwritten, L-150 issued three times | allocated from the accessor plus outstanding reservations | `rdl.py next_lesson_id`, refuses rather than guesses |
| The RDL ladder was prose. Occurrences 1 to 3 reached the operator | described in `CLAUDE.md`, executed nowhere | `scripts/rdl.py` IS the ladder, routed by table to ARCHITECT, hash-chained to the store | SH-10, `RDL_LADDER.json`, `qesis_rdl_defects` |
| A stale `.git/index.lock`, 4 days old, made `git add` fail invisibly | the lander read "nothing staged" as a clean tree and opened two empty pull requests | every lock class enumerated and cleared before staging; staging verified by counting the index | `git_unlock.py`, full L-115 set plus per-branch ref locks |
| Guards checked the wrong property | looked for a running git process while the lock was stale | guards state the property they need | L-154, family `guard_not_executed` |
| Closing summaries were literals | "auto-merge armed on both" printed after the log recorded it failing on both | summaries constructed from measured results | L-155, lander and report generator |
| The daily report depended on the desktop app being open | 7 days produced nothing, then fired as one catch-up burst | runs on a GitHub runner, no cross-repo credential | `.github/workflows/daily-ops-report.yml`, SH-7 |
| Two sessions wrote one working tree and neither could see the other | duplicated CONTROLS entry, undetected | advisory lock refusing a second live writer; CONTROLS uniqueness assertion | L-152, `rdl.py acquire`, `selfheal._assert_controls_unique` |
| The RDL CI gate failed on accumulated history | would have failed every run forever and made every pull request unmergeable | fails only on a family new at rung 3+, or escalated above `RDL_BASELINE.json` | SH-10f, L-158 |
| An instruction that would break a mechanism was obeyed without argument | `--auto` removed, PR 72 became unmergeable | counsel precedes compliance, objection recorded on file | SH-10g, L-159 |
| Two landers with near-identical names, the superseded one was run | revision 2 executed instead of revision 3, repeating the DAG defect | the old file prints a redirect and exits | L-157 |

## Verified, by command, on 2026-08-24

- `verify_ledger_singleton.py --selftest` PASSED, 5 fixtures
- `verify_ledger_singleton.py` PASSED, 140 entries, 140 unique, max L-157, both copies identical
- `rdl.py ci-blocking` exit 0 with a baseline, exit 0 with no baseline, exit **1** against an injected rung-3 regression. Refuse fixture and accept fixture both exercised.
- `verify_action_pinning.py` SEC-1 PASS, 16 of 16 pinned
- `verify_workflow_contract.py` OK, 28 CI scripts, 15 local controls, 15 declared exemptions
- `selfheal.py` 15 unique controls, uniqueness assertion active
- 7 of 7 scripts parse. Zero em dashes in anything written today.

## What is still open, and who genuinely owns it

**Landing status, 2026-08-24T17:37Z, stated as three distinct states.**
`qesis-mcp` PR 72 and `sovereign-infra` PR 36 are **ARMED, not merged**: 42 and
30 files committed and pushed, auto-merge queued behind `qesis-integrity`. They
merge themselves when the check reports green. The lander's summary called this
`ON MAIN`, which was the `success_literal_not_measured` family recurring as a
state label rather than as a sentence, recorded as **L-160**, gate landed in the
same change set, baseline re-accepted with a stated reason.

L-160 is staged in `ops/RDL_PENDING_APPEND.md` and lands on the next lander run.
That is agent work, not the operator's.

**Both release gates are green as of this writing:** ledger singleton exit 0,
RDL delta exit 0. The delta gate proved itself in the process by refusing this
change set once, on a genuine rung 2 to rung 3 escalation, and passing only after
the gate that rung demands had landed.

**Three signatures, and these are the operator's under a named clause.**

1. **D-114**, concurrency discipline for the shared working tree: the advisory
   lock, the CONTROLS uniqueness assertion and the ledger append protocol. Opened
   by L-152 at rung four. A `D-` is signed by the operator, never by an agent.
2. **D-112**, the RDL taxonomy ADR, still `Proposed` since 2026-08-15.
3. **79 Article 14 executions** held, HERALD 40 and COUNSEL 39, oldest
   `2026-07-28T17:12:48Z`, all with expired ruling vintage under D-053.

**Standing debt, owned by ARCHITECT, declared not hidden.** L-083 to L-098 are
cited in code and absent from the ledger, plus L-125. All seventeen are declared
in `ops/LEDGER_GAPS.json` with an owner and a closing condition, so R2 reports a
known debt instead of failing every run. G4 and G7 unchanged since 2026-07-27.
ENTSO-E task `1a48c78d` open since 2026-07-31, a status contradiction rather than
a measurement blocker.

## The honest limit

This makes the ecosystem self-healing against defect families it has already met,
and it now measures deltas rather than punishing its own history. It does not
make it self-correcting against judgement it has never been tested on. SH-6 said
that before today and today did not change it. Two of the eleven faults above
were introduced by the fixes for the others, which is R-2 working as designed:
the moment of highest risk is immediately after a correction.

_Generated by the orchestrator, 2026-08-24. Read-only except this file._
