# Handover, 2026-08-26. Read this first, then run one file.

**For the next session, any model.** Written at 01:10 UTC and revised at 01:45 UTC,
after the second run of lander revision 5 (00:00:52Z), the merges that followed
it, and the first run of revision 6 (01:08:15Z). Everything
below carries the command that establishes it. The evening handover of
2026-08-24 is superseded in full; its five root causes are landed and on `main`.

---

## The one command

```
C:\Users\Lenovo\OneDrive\sovereign-infra\LAND_EVERYTHING_FINAL.bat
```

Revision 6. It judges itself first (`verify_lander_contract.py`), reads the
landing from `LANDING_MANIFEST.json` beside it, installs the pending workflow,
repairs the ledger mirror, runs every gate in BOTH repositories, carries only
the delta this session made onto a branch cut from `origin/main`, pushes, opens
the pull requests, merges by rebase when `qesis-integrity` reports, syncs the
disk to `main`, and ends with the audit. `AUDIT_ECOSYSTEM.bat` still measures
everything and changes nothing. The operator gets one double-click, never a
list of commands to type. L-110.

---

## What is on `main` now, and how it was established

| Fact | Established by |
|---|---|
| qesis-mcp `main` is `24a6a80`, PR 76 merged by rebase at 2026-08-26T00:05:36Z (auto-merge fired the moment the required check passed on `bfe96d1`) | PR 76 page: "merged commit 24a6a80 into main"; tree of `bfe96d1` equals tree of `24a6a80` (`git rev-parse ^{tree}` both `1847880…`) |
| qesis-mcp `main` is green on every workflow it owns: QESIS+ integrity gate #211, Self-heal loop #163, Production integrity probe #374, Branch guard #3 | Actions list filtered `branch:main`, each run "completed successfully" |
| sovereign-infra `main` is `0bd0823`, PR 41 merged by rebase at 2026-08-26T00:34:07Z by this session, G-06 Rule 2-4, operator authorised | PR 41 page: "merged commit 0bd0823 into main"; qesis-integrity green on both `pull_request` and `push` before the merge |
| sovereign-infra `main`: QESIS+ integrity gate #18 green (it was the last red required check of the 19:07Z audit), Continuous compliance #135 green, Branch guard #28 green, **Self-heal loop #35 red** | Actions list filtered `branch:main` |
| The remaining red is one line: `ERROR: Could not open requirements file: requirements.txt`. sovereign-infra has no such file; `selfheal.yml` installs it unguarded and dies in eight seconds before any control runs | run 32913616173 job 98013444992 step 4; `ls sovereign-infra/requirements.txt` absent |
| Ledger: 162 entries, max L-179, sha256 `c770ab5d26e23ee7`, byte-identical in both copies; pending file empty | `rdl_append.py` exit 0 ("appended L-175, L-176, L-177", then "L-178, L-179"); `verify_ledger_singleton.py` exit 0 in both; `ledger_sync.py --check` "already canonical and identical" |
| RDL ladder: 15 families, baseline accepted with reason, 0 regressions | `rdl.py ci-blocking` exit 0 |
| sovereign-infra preflight 7 of 7 in a git-backed replica; qesis-mcp `test_gate.py` 69 of 69; `verify_workflow_contract.py` exit 0 in both with the guarded workflow and exit 1 against the old one | commands as named, replica of both trees |

## What the 00:00:52Z lander run actually did, against what it said

It pushed both repositories (`bfe96d1`, `aca9f53`), updated PR 76, opened PR 41,
closed PR 71, deleted the self-heal litter branch, closed issue 74 with
evidence. Then it printed NOTHING DONE for both. The cause is one line of
Windows PowerShell 5.1: `ConvertFrom-Json` hands a JSON array to the pipeline
as ONE object, `Select-Object -ExpandProperty number` found no such property,
the pull request variable stayed empty, and the merge, the CI feedback and the
MERGED read-back were skipped. **L-176**, second occurrence of
`shell_built_the_query` (L-167 was the jq expansion). The summary was built
from the empty variable. Everything the run claimed not to have done, it had
done.

## The three defects recorded and repaired this session

| Id | Family, rung | What was measured | Repair, and where it is proven |
|---|---|---|---|
| **L-175** | `paired_what_is_not_pairable`, occurrence 3, rung 3 | `selfheal.yml` was paired byte-identical into sovereign-infra and installs `requirements.txt`, which only qesis-mcp has. The heal check failed hourly and on every push before a single control ran. Same move as L-170 and L-171. | The step is guarded (`if [ -f requirements.txt ]`), delivered to `ops/pending_workflows/selfheal.yml` in both repositories; the lander's step 0a installs it. `verify_workflow_contract.py` **C-5** refuses an unguarded `-r FILE` whose file is absent from this repository; three fixtures run before every judgement; the gate is a step of both integrity workflows, so rung 3 is satisfied. `test_gate.py` carries the refuse and accept pair too. |
| **L-176** | `shell_built_the_query`, occurrence 2, rung 2 | The PS 5.1 array quirk above. | The lander's shell parses nothing. `gh_ops.py pr-number` and `pr-state` print one plain string; `landing_manifest.py` prints the branch, the titles and the body. `verify_lander_contract.py` (LC-1 to LC-5, seven fixtures) refuses revision 5 with six findings and accepts revision 6; the lander runs it on itself before any other step; the audit runs it in section A. |
| **L-177** | `disk_snapshot_taken_as_repository`, occurrence 1, rung 1, new family | Revision 5 restored the WHOLE disk snapshot over `origin/main` (`git checkout $save -- .`). Any commit a runner lands on `main` between two clicks would be reverted by the next one with a green summary. Proven in a scratch repository: the revision 5 sequence stages `A R` and writes the runner's `R` back to its old content. Latent, not yet suffered: no runner landing has ever succeeded (L-174), and the setting that allows it is now on in sovereign-infra. | `landing_base.py` names the last commit whose tree is on `origin/main` (normal, rebased, aborted run, push failed, fallback: five fixtures); the lander carries `git diff --name-only <base> <snapshot>` and nothing else, and after MERGED it syncs the disk to `origin/main`. Same scratch fixture under revision 6: the runner's `R` survives, the session's `A N D` land. |

The same move sat in `gh_ops.py amputate`, which closed every open pull request
whose head was not the lander's branch, including the runners' own landings
(`selfheal/<run id>`, `ops/report-<date>`) the moment the setting lets them
exist. It now spares those heads; proven with canned replies (kept 9 and 10,
closed 11).

**The one move behind the three,** stated so the next session can recognise it
before it recurs: something local was taken as the state of something remote.
A file paired without checking the other tree. A shell's rendering taken as the
data. A disk taken as the repository. The 2026-08-24 family (a proxy taken as
the resource, D-115) has a sibling: the local copy taken as the whole.

## Outcome of the 01:08Z click, read from GitHub, and what it exposed

Revision 6 ran end to end on the operator's machine at 2026-08-26T01:08:15Z:
it judged itself, read the manifest, installed the guarded `selfheal.yml` in
both repositories, passed 23 of 23 and 7 of 7 preflight steps, carried 19 and
17 files as the delta, opened PR 77 (qesis-mcp, `6e1dc4c`, auto-merge armed,
CI green, MERGED at `e6865f8`) and PR 42 (sovereign-infra, `1071cb5`, merged by
rebase, CI green, MERGED at `ed16369`), synced both disks to `main`, and ended
with the audit at VERDICT GREEN. On sovereign-infra `main` the Self-heal loop
is green for the first time (#37), so L-175 is proven on the resource.

Reading `main` afterwards, not the log: **Continuous compliance verification
#138 was red on sovereign-infra `main`** (run 32918232104), on
`ops/AUDIT_REPORT.md`: `doctrine violation ['em_dash']`. Two defects, both
recorded at rung 4 of families already on the ladder, both repaired on disk:

| Id | Family | What was measured | Repair, proven |
|---|---|---|---|
| **L-178** | `guard_not_executed`, occurrence 7 | The audit writer rendered its "What is failing" bullets with an em dash, into a file compliance.yml scans with `qesis_agents/style.py`. The 00:13Z NOT GREEN report was the copy the lander committed. | `render_report()` emits `, last line:`; `write_report()` runs `doctrine_hits()` over the rendered text and adds a FAIL row of its own when anything blocks, so the verdict cannot be GREEN over a report that would fail its gate. `audit_ecosystem.py --selftest`: 4 fixtures; `test_gate.py` 71 of 71. |
| **L-179** | `claim_from_proxy_not_resource`, occurrence 8 | The audit asserted only ruleset-required checks and `gh_ops.py proof` filtered names by the words integrity, heal, binding. compliance.yml's job is `verify`, invisible to both. GREEN and "clean" were printed over a red owned check. | `gh_ops.py owned_check_names()` reads each job's name or id from the workflow files; D-116 **rule 6**: a completed check the repository owns is asserted whether or not required; not finished or no run on the commit is reported, never failed. A canned payload with `verify=failure` yields FAIL for sovereign-infra and informational for qesis-mcp. |

## What is NOT done, and is the lander's job on the next double-click

The repairs above, L-178 and L-179 in the ledger (162 entries, max L-179,
sha256 `c770ab5d26e23ee7`, both copies), D-116 rule 6, and the doctrine-clean
`ops/AUDIT_REPORT.md` are on disk in both repositories. `LANDING_MANIFEST.json`
names the branch `fix/land-20260826-audit-owns-its-checks`. Until that click,
sovereign-infra `main` carries a red `verify` (owned, not required) and the
audit on disk, run from the new code, will say so in section C.

## Things only the operator can do, stated plainly, with the clause

- **qesis-mcp, Settings > Actions > General > Workflow permissions: tick "Allow
  GitHub Actions to create and approve pull requests".** Done in sovereign-infra
  on 2026-08-26 (screenshot); not yet seen for qesis-mcp. Without it the
  self-heal loop and the daily ops report cannot land anything from a runner
  there (L-174). G-03, G-04 by analogy: it widens what the Actions token may do.
- **Both repositories, Settings > General > Pull Requests: "Allow auto-merge".**
  qesis-mcp evidently has it (PR 76 auto-merged by rebase). sovereign-infra:
  unknown; the lander merges directly once CI is green, so this costs latency,
  not correctness (SH-10g).
- **Rotate any credential that was in the purged files.** G-03, G-04.
- **Disconnect the Google Cloud Build triggers** (both repositories). Every push
  produces a failing `cloudrun-…europe-west1…` check; not required, listed as
  informational (D-116 rule 3); L-044 rejected Cloud Build. GCP console
  credential, G-03.
- **Delete the desktop scheduled task `qesis-daily-ops-report`.** It writes into
  the decoy stub; the report runs in Actions under SH-7.

## Standing instructions, unchanged and still binding

- Never run any git command from the analysis mount, read-only included. L-122,
  L-123, L-150. This session ran none; every git act is in the lander, and the
  lander's own git logic was proven in a scratch repository here.
- `C:\Users\Lenovo\sovereign-infra` is an empty decoy stub. The repository is
  `C:\Users\Lenovo\OneDrive\sovereign-infra`. L-143.
- Never mark an item for the operator without naming the clause. SH-4.
- Counsel precedes compliance. SH-10g.
- Auto-merge cannot resolve conflicts. On DIRTY, close and re-cut. L-165.
- Lesson ids come from `verify_ledger_singleton.py --json` plus the pending
  reservations, never from the tail. L-151. This session took L-175 to L-177
  that way, through `rdl.py record`.
- A workflow file reaches `.github/workflows` only through the lander's step 0a.
  Deliver revisions to `ops/pending_workflows/`.
- The lander's shell parses nothing (L-176) and the lander lands only the delta
  (L-177). `verify_lander_contract.py` refuses anything else, and the lander
  runs it on itself first.

## The honest limit

The fixtures prove the gates refuse and accept what they claim. The lander's
own delta block, verbatim, was executed in PowerShell 7.4 against a scratch
repository in three cases (a runner commit plus session changes with a path
containing a space; exactly one changed file; nothing changed) and did what
revision 6 claims. That is 7.4, not the operator's 5.1; the one 5.1 difference
known to matter, a single-element array unrolling to a string, is handled
explicitly at both call sites. None of this proves the next defect will be one
of these families. The first revision 6 run on the operator's machine is the
test of the rest; its log is the evidence. SH-6.

_Written by the Cowork session of 2026-08-26 from a sandbox replica of both
working trees and from GitHub read through the operator's browser session;
every exit code above was observed, not inferred._
