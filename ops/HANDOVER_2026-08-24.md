# Handover, 2026-08-24 evening. Read this first, then run two files.

**For the next session, any model.** Rewritten at 21:30 UTC after the evening
audit (`ops/AUDIT_REPORT.md`, 19:07Z, verdict NOT GREEN, 4 failing) was
root-caused and repaired. Everything below carries the command that establishes
it. The morning handover of the same date is superseded in full.

---

## The two commands, in this order

```
1.  C:\Users\Lenovo\OneDrive\sovereign-infra\AUDIT_ECOSYSTEM.bat
    Measures everything. Changes nothing. Writes ops\AUDIT_REPORT.md in both
    repositories. Every row now carries the PREDICATE that decided its verdict.

2.  C:\Users\Lenovo\OneDrive\sovereign-infra\LAND_EVERYTHING_FINAL.bat
    Repairs the ledger mirror, runs every gate in BOTH repositories, aborts
    before touching git if any refuses, lands the work, waits for CI on the
    pushed commit, and ends by running the audit. Revision 5.
```

The operator gets one double-click, never a list of commands to type. L-110.

---

## What the 19:07Z audit showed, and what each red actually was

| Audit row | What it said | Root cause, measured | Fixed by |
|---|---|---|---|
| 1, 6 | qesis-mcp singleton FAIL, sovereign-infra singleton PASS on the same two files | The gate's sibling list was a fixed list of sovereign-infra paths. From sovereign-infra the first existing path was the ledger itself, so R3 compared the file with itself and printed "sibling agrees". The real drift was one byte: the qesis-mcp copy had lost its trailing newline at 18:49Z through a direct write that bypassed `rdl_append.py`. | `verify_ledger_singleton.py` resolves the OTHER repository from the checkout's identity, reports self-resolution as DEGRADED, names the delta, ten fixtures. `scripts/ledger_sync.py` repairs mechanical drift losslessly. L-169. |
| 9, 10 | sovereign-infra bootstrap STALE, preflight FAIL, and `qesis-integrity` red on its `main` | `ECOSYSTEM_STATE.json` carries `repository`, the checkout directory name, which differs between the two repositories by construction; the same file was mirrored into both. | `repository` is volatile; `build_ecosystem_state.py --selftest` accepts volatile drift and refuses contract drift; the selftest is a step in both integrity workflows and in `test_gate.py`. L-170. |
| 5 | qesis-mcp preflight FAIL, "65/66 gate behaviours" | Downstream of row 1: `test_gate.py` runs the live singleton gate. | Row 1. |
| 16 | sovereign-infra `heal: failure` on `main` | `selfheal.py` imported `scripts/kill_switch.py` unconditionally; it existed only in qesis-mcp, so the runner crashed before reading a control. Behind that, `verify_workflow_contract.py` C-4 flagged twelve served-index scripts as "not tracked" in a repository they never belonged to, and `verify_chain.py` exit 2 (store gitignored on the runner) escalated CRITICAL hourly. | `load_kill_switch()` tolerates absence and honours both channels; kill switch paired into sovereign-infra; C-4 distinguishes scope from untracked; exit 2 degrades as `store_not_reachable`; `selfheal.py --selftest`. L-171. |
| 13, 16 | "main check conclusions" PASS while the value listed `failure` | The audit set the row's verdict from `gh`'s exit code, an operation, never from the value. | `audit_ecosystem.py` rebuilt on two row kinds, gate and measurement; required checks read from the branch ruleset; DIRTY pull requests fail. D-116. L-172. |

| lander 20:31Z | sovereign-infra preflight 6 of 7: "C-4 scripts/kill_switch.py is NOT TRACKED" | C-4 read git's index as the answer to "will CI have this file"; a script new in the change set is never in the index before the lander stages it. The replica that certified the gate had no `.git`, so the gate's filesystem fallback ran instead of the branch that failed on the host. | C-2/C-4 ask `git check-ignore`: tracked, or untracked-and-not-ignored (staged by `git add -A`), or absent (scope); only ignored-on-disk fails. Proven both ways in a git-backed replica. L-173, D-115 amended. |

**The one move behind all five:** a property was asserted from something other
than the thing the property is about. A file compared with itself. A checkout
name compared as a contract. A runner declared repository-neutral without being
run in the second repository. A listing's exit status read as the listing's
content. Same family as the morning; the repairs this time are fixtures, not
sentences.

## What is true now, and how it was established

| Fact | Command, run in a sandbox replica of both working trees AND re-run on the operator's own machine against the files on disk (device shell, no git) |
|---|---|
| Ledger is one file, 157 entries, max L-174, sha256 `c9a259e7c6369ce6`, byte-identical in both copies | `verify_ledger_singleton.py` exit 0 in both; `ledger_sync.py` "already canonical and identical" |
| L-166 to L-174 are IN the ledger; `ops/RDL_PENDING_APPEND.md` is empty | `rdl_append.py` exit 0, three appends |
| RDL ladder: 14 families, baseline accepted, 0 regressions | `rdl.py ci-blocking` exit 0 in both |
| qesis-mcp preflight: 23 of 23 CI steps pass; `test_gate.py` 67 of 67 | `preflight.py` exit 0; `test_gate.py` exit 0 |
| sovereign-infra preflight: 7 of 7 CI steps pass (four new fixture steps), in a git-backed replica with `kill_switch.py` untracked exactly as on the host | `preflight.py` exit 0; `verify_workflow_contract.py` exit 0 with "untracked, not ignored: kill_switch.py" |
| Self-heal loop: qesis-mcp GREEN, sovereign-infra DEGRADED with 0 escalations (chain store not reachable off the host, declared) | `selfheal.py --dry-run` exit 0 in both |
| Vercel production serves `main` (8775fc5), chain VERIFIED, 752 entries, 0 breaks | live `/health`, 19:07Z |
| PR 71 (qesis-mcp) conflicts: `refs/pull/71/merge` absent on GitHub. PR 76 is mergeable: `refs/pull/76/merge` present | `git ls-remote origin 'refs/pull/7*'` |

## What is NOT done, and is the lander's job on the next double-click

1. **Nothing above is on GitHub yet.** It is on disk in both repositories.
   `LAND_EVERYTHING_FINAL.bat` pushes it as one commit per repository on
   `fix/land-20260824-consolidated`, updates PR 76, opens the sovereign-infra
   pull request, and merges by rebase when `qesis-integrity` reports.
2. **PR 71 is a zombie** (DIRTY, auto-merge armed, can never fire). Its content
   is on `main` already through later commits. `gh_ops.py amputate` closes it;
   the lander calls it.
3. **sovereign-infra `main` stays red until the merge lands**, because the red is
   the committed `ECOSYSTEM_STATE.json` and the crashing runner, both fixed on
   disk only.
4. **CI has not yet proved the new steps.** They passed in a replica with the
   same Python step list; the real proof is the run on the pushed commit, which
   `ci_feedback.py --sha` now pins to that commit and writes into the repository.

## Found on GitHub itself, 2026-08-24 evening (read from the run logs, L-174)

Self-heal loop run 132 (qesis-mcp, 00b0c95) reached GREEN with one repair,
pushed `selfheal/32763154920`, then died: *GitHub Actions is not permitted to
create or approve pull requests*. The repository setting is off in both
repositories. Consequence: no runner-side landing has ever succeeded, so SH-7
("nothing depends on the operator's machine") has been true on paper only, for
the loop and for the daily ops report alike. `selfheal.yml` now deletes the
branch it cannot open a pull request for and names the setting; `gh_ops.py`
removes the existing litter. Turning the setting on is below.

## Things only the operator can do, stated plainly, with the clause

- **Two repository settings, both repositories, two clicks each.** The agent
  is refused on GitHub settings pages by policy, and rightly: both widen what
  the Actions token may do (G-03, G-04 by analogy).
  1. Settings > Actions > General > Workflow permissions: tick **Allow GitHub
     Actions to create and approve pull requests**, Save. Without it the
     self-heal loop and the daily ops report cannot land anything from a runner.
  2. Settings > General > Pull Requests: tick **Allow auto-merge**. Without it
     `gh pr merge --auto` is refused and every merge waits for a human (L-159,
     SH-10g). The lander now merges directly once CI is green as a fallback, so
     this one costs latency, not correctness.

- **Rotate any credential that was in the purged files.** Deleting a file does
  not invalidate the secret. G-03, G-04.
- **Disconnect the Google Cloud Build trigger.** Every push to both repositories
  produces a failing `cloudrun-…europe-west1…` check run on `main` (19:07Z audit,
  rows 13 and 16). L-044 rejected Cloud Build as a duplicate of a pipeline that
  already runs free and green; the trigger was never removed. It is not a
  required check, so it blocks nothing, and the audit lists it as informational
  (D-116 rule 3). Removing it needs the GCP console credential, which no agent
  may hold: G-03, G-04. Cloud Build, Triggers, delete the trigger for each repo.
- **Delete the desktop scheduled task `qesis-daily-ops-report`.** It lives in the
  Claude desktop app, not in any repository, and it writes pointer files into
  the decoy stub `C:\Users\Lenovo\sovereign-infra\ops\reports\`. The report is
  generated in GitHub Actions under SH-7 (`daily-ops-report.yml`); the desktop
  task is a reminder, not a schedule, and no agent can reach the app's settings.

## Standing instructions, unchanged and still binding

- Never run any git command from the analysis mount, read-only included. L-122,
  L-123, L-150. This session ran none; every git act is in the lander.
- `C:\Users\Lenovo\sovereign-infra` is an empty decoy stub. The repository is
  `C:\Users\Lenovo\OneDrive\sovereign-infra`. L-143. `CLAUDE.md` section 2 now
  says so; it named the stub until this evening.
- Never mark an item for the operator without naming the clause. SH-4.
- Counsel precedes compliance. SH-10g.
- Auto-merge cannot resolve conflicts. On DIRTY, close and re-cut. L-165.
- Lesson ids come from `verify_ledger_singleton.py --json` plus the pending
  reservations, never from the tail. L-151. This session took L-169 to L-172 that
  way, through `rdl.py record`.

## The honest limit

The fixtures prove the gates refuse and accept what they claim. They do not
prove the next defect will be one of these families. SH-6.

_Written by the evening session, 2026-08-24, from a sandbox replica of both
working trees; every exit code above was observed there, not inferred._
